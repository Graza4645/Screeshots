name: Playwright Tests CI/CD Pipeline

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]
  workflow_dispatch:

jobs:
  test-driver:
    name: Driver Salary Validation
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - run: npx playwright install chromium --with-deps
      - name: Run Driver Salary Tests
        run: npx playwright test driverSalaryValidation.spec.js
        continue-on-error: true
      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-driver
          path: tests/Hiring_Tabulation_SalaryCalculator/screenshots/DriverSalary/
          retention-days: 30
          if-no-files-found: ignore

  test-esic:
    name: ESIC Validation
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - run: npx playwright install chromium --with-deps
      - name: Run ESIC Tests
        run: npx playwright test esicValidation.spec.js
        continue-on-error: true
      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-esic
          path: tests/Hiring_Tabulation_SalaryCalculator/screenshots/ESIC/
          retention-days: 30
          if-no-files-found: ignore

  generate-report:
    name: Generate Allure Report
    needs: [test-driver, test-esic]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - name: Generate Allure report
        run: |
          mkdir -p allure-results
          npx allure generate allure-results --clean -o allure-report
      - name: Upload Allure report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report/
          retention-days: 30
